table:
  name: match_map_demos
  schema: public
object_relationships:
  - name: match
    using:
      foreign_key_constraint_on: match_id
  - name: match_map
    using:
      foreign_key_constraint_on: match_map_id
array_relationships:
  - name: clip_render_jobs
    using:
      foreign_key_constraint_on:
        column: match_map_demo_id
        table:
          name: clip_render_jobs
          schema: public
  - name: match_clips
    using:
      foreign_key_constraint_on:
        column: match_map_demo_id
        table:
          name: match_clips
          schema: public
  - name: demo_sessions
    using:
      foreign_key_constraint_on:
        column: match_map_demo_id
        table:
          name: match_demo_sessions
          schema: public
computed_fields:
  - name: download_url
    definition:
      function:
        name: demo_download_url
        schema: public
  - name: playback_url
    definition:
      function:
        name: demo_playback_url
        schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - id
        - match_id
        - match_map_id
        - size
        - playback_size
        - total_ticks
        - tick_rate
        - duration_seconds
        - round_ticks
        - metadata_parsed_at
        - map_name
        - workshop_id
        - cs2_build
        - kills
        - bombs
        - players
        - created_at
      computed_fields:
        - download_url
        - playback_url
      filter: {}
      allow_aggregations: true
    comment: ""
event_triggers:
  - name: match_map_demo_events
    definition:
      enable_manual: false
      insert:
        columns: '*'
      update:
        columns:
          - metadata_parsed_at
          - file
    retry_conf:
      interval_sec: 10
      num_retries: 3
      timeout_sec: 60
    webhook: '{{HASURA_GRAPHQL_EVENT_HOOK}}'
