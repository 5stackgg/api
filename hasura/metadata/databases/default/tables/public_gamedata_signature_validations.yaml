table:
  name: gamedata_signature_validations
  schema: public
object_relationships:
  - name: game_version
    using:
      foreign_key_constraint_on: build_id
select_permissions:
  - role: administrator
    permission:
      columns:
        - id
        - build_id
        - branch
        - status
        - results
        - validated_at
      filter: {}
      allow_aggregations: true
    comment: ""
